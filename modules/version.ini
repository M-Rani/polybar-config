[module/version]
type = custom/ipc
initial = 1 

hook-0 = echo "$(uname -r | cut -d\- -f1)"

format = <output>
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-padding = 1
